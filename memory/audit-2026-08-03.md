# Audit sécurité 2026-08-03 — ND1 → ND11

> **Statut du fichier** : reconstruit le 16/08/2026 depuis SECURITY.md et
> MEMOIRE_CAISSE.md (Section 03/08/2026). Détails d'exécution bruts
> (logs de vérification) **⚠️ à compléter par Yannick** si nécessaires.

## Contexte
Audit global du projet le 03/08/2026 (5 agents Explore en parallèle :
cartographie, sécurité, routes & navigation, code applicatif, conformité
règles). 11 dettes détectées (ND1-ND11), corrigées le jour même. C'est aussi
la date d'ouverture du CHANTIER 1 — Fondations.

## Détails des corrections

| # | Fix | Dette | Correction | Fichier(s) |
|---|-----|-------|-----------|------------|
| Fix #1 | ND1 | XSS `document.write` | Helper `escapeHtml` appliqué sur toutes les interpolations `${...}` (anti-XSS sur `printRapport()`, `printTicket()`) | `src/lib/utils.ts`, `src/views/screens/Caisse.tsx`, `Tickets.tsx` |
| Fix #2 | Dette #1 | `.env` tracking | Vérification : l'audit initial était infirmé — `.env` non tracké, A.1 avait bien fonctionné | — |
| Fix #3 | ND3 | `/client` sans `beforeLoad` RBAC | Guard ajouté | `src/routes/_authenticated/client/route.tsx:15-33` |
| Fix #4 | ND5 | Validation faible `/platform` | 7 helpers + bornes sur `createTenant`/`sendInvitation`/`addAdmin`/`saveTenantSettings` | `src/routes/_authenticated/platform/index.tsx` |
| Fix #5 | ND4 | `requireSupabaseAuth` mort | Suppression YAGNI (109 lignes) | `src/integrations/supabase/auth-middleware.ts` (supprimé) |
| Fix #6 | ND6 + bis + ter | Casts `as any` orphelins + `src/hooks/` vide + dépendance morte | 5 casts supprimés + dossier supprimé + `@lovable.dev/cloud-auth-js` retirée | `Caisse.tsx`, `package.json` |
| Fix #7 | ND8 | `handleImportFile` faible | 3 type guards (`isValidSession`/`isValidTicket`/`isValidPoste`) + whitelist `paymentMethod` + `Number.isFinite` sur montants | `src/views/screens/Reglages.tsx` |
| Fix #8 | ND7 | `merge` Zustand sans validation | Type guards sur sessions/tickets/postes avant `setState` | `src/store/useStore.ts` |
| Fix #9 | ND2 | PII en clair localStorage | **Phase A** : anonymisation 7j (alignée `TICKET_VALID_MS`) à chaque boot. **Phase B (à venir)** : chiffrement AES-GCM Web Crypto + clé IndexedDB. Limite honnête documentée : n'arrête PAS un XSS actif | `src/store/useStore.ts`, `src/lib-app/secure-storage.ts` (créé) |
| Fix #10 | ND11 | Conflit lockfiles (npm vs pnpm) | `package-lock.json` supprimé, `pnpm-lock.yaml` conservé, `packageManager: pnpm@9` ajouté | `package.json` |

## Vérifications
- `npx tsc --noEmit` exit 0 sur chaque Fix.
- `npx vite build` exit 0 sur chaque Fix.
- Aucune régression introduite.

## Documents produits par l'audit
- `SECURITY.md` (racine) — modèle de menace, RBAC, RLS, PII, validation
  entrées, conformité RGPD, procédure de signalement (l.169-175).

## Dettes restantes à l'issue de l'audit (03/08/2026)
- #1 rotation `SUPABASE_SERVICE_ROLE_KEY` — action Yannick.
- #4 licences — backlog.
- #5 isolation RLS (`is_platform_admin` OR-é) — à re-trancher phase C.
- #23 `loyalty_points` UI — backlog.
- #25 `audit_log` — à planifier.

## ⚠️ À compléter (Yannick)
- Logs de vérification bruts détaillés de chaque Fix (les sources ne
  conservent que le résumé "tsc exit 0 / vite build exit 0").
- Éventuels constats d'audit intermédiaires des 5 agents (rapports bruts).

## Liens
- SECURITY.md (§5.3 = résumé ND1-ND11)
- MEMOIRE_CAISSE.md — Section 03/08/2026 (tableau des Fix) + journal session
  03/08→12/08 (ND.0, ND.1-ND.11)
- `memory/dette-technique.md` (dettes actives/fermées consolidées)
